services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: perfiapp
      POSTGRES_PASSWORD: perfiapp_dev_only
      POSTGRES_DB: perfiapp_kb
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
